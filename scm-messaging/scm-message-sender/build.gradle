description = 'SCM Message Sender'

apply plugin: 'groovy'

dependencies {
    compile project(":scm-messaging:scm-messages-http")

    compile libraries.httpClient
    compile libraries.commonLang3
    compile libraries.jacksonDatabind

    testCompile libraries.spock
}